# 数据对象生命周期

为项目、巡查问题、遥感图斑、补贴申报、确权记录、保险记录、农资主体等分配对象编号，记录 `object_id,name,producer,storage,consumers,statuses,retention,related_functions,related_flows`。

每个对象说明产生位置、维护角色、查看/编辑页面、审核前后状态、是否同步其他库、异常提示和结束后查询位置。跨系统对象在独立手册中保留关联说明，在整合版集中索引。
